#!/bin/sh
# Portable public clean-clone gate. All data fixtures are synthetic and temporary.
set -eu
npm ci
npm run check
npx playwright install chromium
npm run test:browser -- --timeout 120000
npm audit --omit=dev --audit-level=high
